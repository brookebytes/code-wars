//Grasshopper - Check for factor - 8kyu
// The function should test if the factor is a factor of the base. Return true if it is factor or false if it is not

function checkForFactor (base, factor) {
  return base%factor==0;
}
