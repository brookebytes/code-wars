//Find Nearest square number - 8kyu
// Your task is to find the nearest square number of a positive integer. If the n is already the perfect square you need to just return n.

const nearestSq = n => Math.round(Math.sqrt(n))**2;
